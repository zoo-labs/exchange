# Zoo Exchange build-time env — Vite picks up from apps/web/.env.local.
# Runtime /config.json (K8s ConfigMap) overrides for per-env RPC/IAM.
VITE_BRAND_NAME="Zoo Exchange"
VITE_DEFAULT_CHAIN_ID=200200
VITE_CLIENT_ID=zoo-exchange
VITE_IAM_ORG=zoo
VITE_ENVIRONMENT=main
VITE_DISABLE_TURNSTILE=false
