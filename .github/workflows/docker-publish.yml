name: Docker

on:
  workflow_dispatch:
  push:
    branches: [main, dev, test]
    tags: ['v*']

permissions:
  contents: read
  packages: write

jobs:
  docker:
    uses: hanzoai/.github/.github/workflows/docker-build.yml@main
    with:
      image: ghcr.io/zooai/exchange
      # Our own ARC runners only — no GitHub-hosted.
      # Zoo org has its own ARC scale set (zoo-build-linux-amd64).
      # arm64 disabled until zoo stands up its own arm64 ARC.
      runner-amd64: zoo-build-linux-amd64
      platforms: linux/amd64
    secrets: inherit

  notify-universe:
    needs: docker
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: zoo-build-linux-amd64
    steps:
      - uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.UNIVERSE_PAT }}
          repository: zooai/universe
          event-type: image-published
          client-payload: |
            {
              "service": "exchange",
              "image": "ghcr.io/zooai/exchange",
              "tag": "${{ github.ref_name }}",
              "sha": "${{ github.sha }}"
            }
