name: Docker

on:
  workflow_dispatch:
  push:
    branches: [main, dev, test]
    tags: ['v*']

permissions:
  contents: read
  packages: write

jobs:
  docker:
    uses: hanzoai/.github/.github/workflows/docker-build.yml@main
    with:
      image: ghcr.io/zooai/exchange
      # Our own ARC runners only — no GitHub-hosted.
      # Zoo org's ARC runner registered without scale-set labels;
      # match via 'self-hosted' until ARC controller config is fixed.
      runner-amd64: self-hosted
      platforms: linux/amd64
    secrets: inherit

  notify-universe:
    needs: docker
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: self-hosted
    steps:
      - uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.UNIVERSE_PAT }}
          repository: zooai/universe
          event-type: image-published
          client-payload: |
            {
              "service": "exchange",
              "image": "ghcr.io/zooai/exchange",
              "tag": "${{ github.ref_name }}",
              "sha": "${{ github.sha }}"
            }
